import { VocalNumbersPipe } from './vocal-numbers.pipe';

describe('VocalNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
